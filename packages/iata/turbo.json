{
  "$schema": "https://turbo.build/schema.json",
	"extends": ["//"],	
	"pipeline": {
		"check": {
			"inputs": ["src/**/*.ts", "build/**/*.ts"],
			"outputs": ["src/**/*.ts", "build/**/*.ts"],
			"outputMode": "errors-only"
		}
	}
}